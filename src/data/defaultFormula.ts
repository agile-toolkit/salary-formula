/**
 * Default Management 3.0–style factor stack used when resetting the calculator.
 * Re-exported for tooling/tests; runtime defaults live in `presets.ts`.
 */
export { DEFAULT_FACTORS } from './presets'
